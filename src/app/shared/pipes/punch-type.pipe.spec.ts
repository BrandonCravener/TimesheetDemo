import { PunchTypePipe } from './punch-type.pipe';

describe('PunchTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PunchTypePipe();
    expect(pipe).toBeTruthy();
  });
});
